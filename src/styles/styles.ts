import styled from "styled-components";
import background from "../../public/fundo site.png";

export const Main = styled.main`
    width: 100%;
    min-height: 100vh;
    background-image: url(${background});
    background-repeat: no-repeat;
    background-size: cover;
`;